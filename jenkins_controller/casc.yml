jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "panda"
          name: "Panda"
          password: "panda"
  authorizationStrategy:
    globalMatrix:
      permissions:
        - "Overall/Administer:panda"
  numExecutors: 0